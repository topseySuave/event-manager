const Helpers = {
    equals: (val1, val2) => {
        // val1 = Helpers.toString(val1);
        // val2 = Helpers.toString(val2);
        return val1 === val2;
    },

    // toString: (str) => {
    //     let strArr = str.split('');
    //     return strArr.join('').toLowerCase();
    // }
};
module.exports = Helpers;